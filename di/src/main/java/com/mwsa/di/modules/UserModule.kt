package com.mwsa.di.modules

import org.koin.dsl.module

val userModule = module {
/*
    factory { PropertyApi() }

    factory<PropertyRemoteDataSource> { PropertyRemoteDataSourceImpl(get()) }
    factory<PropertyRepository> { PropertyRepositoryImpl(get()) }
    factory<PropertyValidator> { PropertyValidatorImpl() }

    factory { GetPropertyUseCase(get()) }
    factory { GetPropertiesUseCase(get(), get()) }

    viewModel { PropertiesViewModel(get()) }*/
}
